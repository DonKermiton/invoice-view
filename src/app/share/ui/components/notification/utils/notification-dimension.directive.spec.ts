import { NotificationDimensionDirective } from './notification-dimension.directive';

describe('NotificationDimensionDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificationDimensionDirective();
    expect(directive).toBeTruthy();
  });
});
