import { StepperPrevPageDirective } from './stepper-prev-page.directive';

describe('StepperPrevPageDirective', () => {
  it('should create an instance', () => {
    const directive = new StepperPrevPageDirective();
    expect(directive).toBeTruthy();
  });
});
